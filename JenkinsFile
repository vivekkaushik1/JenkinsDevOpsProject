def artifactname = "JenkinsDevOpsProject-0.0.1-SNAPSHOT-feature-devops-1.33.jar"
def repoName = "JenkinsDevOpsProject"
def pipelineName = "SunilDevOps-Multibranch-Project"
def semanticVersion = "${env.BUILD_NUMBER}.0.0"
def packageName = "JenkinsDevOpsProject-package_feature-devops-1.33_${env.BUILD_NUMBER}"
def version = "${env.BUILD_NUMBER}.0"
def pkgName = "JenkinsDevOpsProject-package-feature-devops-1.33"

pipeline {
  agent any
  tools {
       maven 'Maven'
   }
  stages {
       stage('Build') {
           steps {
              
              sh 'mvn -B -DskipTests clean compile'

           }
       }
       stage('Test') {
           steps {
              
              sh 'mvn test'
           }
           post {
             always {
                junit "**/target/surefire-reports/*.xml"
             }
           }
       }
 }
 
}
